package com.circuitstitch.deferno.ios

/**
 * Scaffold marker for the `app:iosApp` umbrella framework (issue #8). Replaced when
 * the framework starts exporting the feature components for the SwiftUI app to host.
 */
internal const val SCAFFOLD: String = "app:iosApp"
