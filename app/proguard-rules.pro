# Add project-specific ProGuard / R8 rules here.
# https://developer.android.com/build/shrink-code
#
# Keep rules for reflection-based libraries (e.g. serialization, DI) go here as
# they are introduced. The defaults from proguard-android-optimize.txt already
# cover the Android framework and AndroidX.
