<?xml version="1.0" encoding="utf-8"?>
<!-- ADR-0047 stagingDebug variant: same full-bleed adaptive-icon background as main, recolored to a
     distinct muted teal so the Deferno Dev launcher icon is visually separable from prod. Only the
     fillColor differs; the flame foreground + monochrome layers stay inherited from main. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path
        android:fillColor="#12333A"
        android:pathData="M0,0h108v108h-108z" />
</vector>
