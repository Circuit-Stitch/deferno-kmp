-- The local source-of-truth Chore table (ADR-0001, #71). Mirrors habitEntity (a recurring definition
-- governed by definition_state) and adds the chore-specific `cadence_mode`. Group/rotation is deferred
-- (ADR-0015), so there is no group column. Adapter-free primitives; conversion in ChoreEntityMapping.kt.
CREATE TABLE choreEntity (
    id               TEXT NOT NULL PRIMARY KEY,
    org_slug         TEXT NOT NULL,
    owner_org_id     TEXT,
    ref              TEXT,
    sequence         INTEGER,
    title            TEXT NOT NULL,
    definition_state TEXT NOT NULL,
    recurrence_type  TEXT,
    recurrence_days  TEXT NOT NULL DEFAULT '',
    cadence_mode     TEXT,
    labels           TEXT NOT NULL DEFAULT '',
    parent_id        TEXT,
    complete_by      TEXT,
    pinned           INTEGER NOT NULL DEFAULT 0,
    date_created     TEXT NOT NULL,
    deleted_at       TEXT,
    hydration_state  TEXT NOT NULL,
    description      TEXT,
    series_id        TEXT,
    -- Appended for migration 2->3 (#348) — ADD COLUMN appends, so it lives at the table's end.
    deadline_time_of_day TEXT,
    -- Appended for migration 9->10 (#290) — server-derived dependency flags (a recurring row inherits
    -- `blocked` from a blocked ancestor). Nullable so ADD COLUMN leaves pre-migration rows NULL.
    blocked    INTEGER,
    is_blocker INTEGER,
    -- Appended for migration 17->18 (#375) — the soft target date + the urgency bucket, both riding
    -- every read and carried on all four kinds (peers of complete_by/pinned). `priority` stores the
    -- Priority enum NAME, like definition_state. Nullable so ADD COLUMN leaves pre-migration rows NULL
    -- (decoded as "no target" / Priority.Normal until the next refresh repopulates).
    target_date TEXT,
    priority    TEXT,
    -- Appended for migration 18->19 (#382) — the rest of the recurrence rule; see HabitEntity.sq for the
    -- full rationale (flat primitives not a JSON blob; one `recurrence_interval` for both wire spellings
    -- of the cycle; the anchor trio kept separate from the yearly month/day; NULL end_type = the Never
    -- bound; raw_type preserves an unmodellable cadence). The chore is where this bug was reported:
    -- `every_n_days` collapsed to "Unknown" and both live every-N-days chores were unrecoverable.
    recurrence_interval       INTEGER,
    recurrence_anchor_type    TEXT,
    recurrence_anchor_day     INTEGER,
    recurrence_anchor_nth     INTEGER,
    recurrence_anchor_weekday TEXT,
    recurrence_month          INTEGER,
    recurrence_day            INTEGER,
    recurrence_rrule          TEXT,
    recurrence_end_type       TEXT,
    recurrence_end_date       TEXT,
    recurrence_end_count      INTEGER,
    recurrence_raw_type       TEXT
);

CREATE INDEX choreEntity_owner_org_id ON choreEntity(owner_org_id);
CREATE INDEX choreEntity_deleted_at ON choreEntity(deleted_at);

selectAllActive:
SELECT * FROM choreEntity WHERE deleted_at IS NULL ORDER BY sequence;

selectById:
SELECT * FROM choreEntity WHERE id = ?;

-- Just the ids — the /items reconcile diffs these against a fresh snapshot to purge absent rows (#226).
selectAllIds:
SELECT id FROM choreEntity;

-- Just the "light switch" — the kind-neutral definition-state read (ADR-0053 decision 4, #390); see
-- HabitEntity.sq for the full rationale, including why the query name is kind-qualified.
selectChoreDefinitionStates:
SELECT id, definition_state FROM choreEntity WHERE deleted_at IS NULL;

selectChoreDefinitionStateById:
SELECT definition_state FROM choreEntity WHERE id = ? AND deleted_at IS NULL;

-- Binds the whole row as one parameter, so the generated signature is `insertOrReplace(ChoreEntity)` and
-- adding a column never touches the Kotlin call site.
insertOrReplace:
INSERT OR REPLACE INTO choreEntity VALUES ?;

deleteById:
DELETE FROM choreEntity WHERE id = ?;
